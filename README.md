# Homeworks 

0x01. ASM vs HLL
1. Реализовать программу рекурсивного вычисления факториала заданного числа на ASM
2. Реализовать программу итеративного вычисления факториала заданного числа на ASM

 0x02. Vulns & expls/PoCs. Bugs. BOF. ShCode4SBOF 

1. Реализовать эксплойт переполнения буфера, приводящий к вызову интерпретатора /usr/bin/python или /usr/bin/perl внутри уязвимой программы vuln_stack
2. Реализовать эксплойт переполнения буфера, приводящий к зависанию уязвимой программы vuln_stack (бесконечный цикл при попытке возврата из функции foo)
3. Реализовать эксплойт переполнения буфера, приводящий к досрочному выходу из уязвимой программы vuln_stack (вызов exit при попытке возврата из функции foo)

0x03. InfoSec. BOF in .bss & heap. FSV & misc MemCorr 


1. Реализовать эксплойт вызова интерпретатора (к
примеру, /usr/bin/python или /usr/bin/perl) через
переполнение буфера в .bss и куче с
использованием переменной окружения

2. Реализовать эксплойт зависания программы
(бесконечный цикл при попытке вызова функции
prt) через переполнение буфера в .bss и куче с
использованием переменной окружения

3. Реализовать эксплойт досрочного выхода из
программы (вызов exit при попытке вызова
функции prt) через переполнение буфера в .bss и
куче с использованием переменной окружения


0x04. Backdoors. Passwd crackers 

1. Реализовать бэкдор на основе UNIX-файловых
сокетов

2. Реализовать bruteforce-переборщик паролей для
взлома zip-архива:
✓ Исходные данные. Имеется запароленный zip-архив.
✓ Ограничения. Пароль представляет собой
последовательность буквенных символов в нижнем
регистре ('a'-'z') длиной не более 4 символов.
✓ Цель. Подобрать пароль к zip-архиву. Поиск пароля
необходимо производить последовательно (a, b, ..., z,
aa, ab, ..., az, ..., za, zb, ..., zz, ..., aaaa, aaab, ..., zzzz).


3. Реализовать параллельный bruteforce-переборщик
паролей для взлома zip-архива


0x05. Prof. Dbg 

1. Выполнить запуск программы ./login_exec из-под
ltrace с фильтрацией по вызовам malloc+free,
проанализировать возвращаемые значения функций и
передаваемые на их вход значения аргументов,
сформулировать и обосновать наличие возможной
MemCorr уязвимости.


0x06. Injection 

1. Выполнить подмену dll в процессе программы dll_inject таким
образом, чтобы эта программа предоставляла доступ к
python-интерпретатору.


2. Изменить дамп памяти процесса server.py таким образом, чтобы
в файл /tmp/out данные (логин, пароль) записывались в
исходном виде (без хеширования).

3. Взломать процесс программы test_prog при помощи техники
"got.plt hijacking" таким образом, чтобы эта программа (1)
предоставляла доступ к python-интерпретатору 

4. Произвести инъекцию кода (при помощи одного из трех
алгоритмов) в процесс запущенной программы simple_loop (или
simple_loop_dl), приводящую к запуску python-интерпретатора.



<!-- 0x07. Injectors 

0x08. RevEng. Decomp 

0x09. CRA 

0x0A. ELF. Viruses & worms 

0x0B. (Virtual|Container)ization. Escape attacks 

0x0C. eBPF. Cloudsec. Falco 

0x0D. CNA. RCE via POST2CGI 

0x0E. Sniffers. NIDS & HIDS. Insertion & evasion attacks 

0x0F. Net. attack detection. NIDS dev. based on CI methods -->